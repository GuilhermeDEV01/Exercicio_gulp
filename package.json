{
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "gulp-sourcemaps": "^3.0.0",
    "gulp-uglify": "^3.0.2",
    "gulp-obfuscate": "^0.2.9",
    "gulp-imagemin": "^7.1.0"
  },
  "scripts": {
    "comprimeImagens": "gulp.src('./source/images/*').pipe(imagemin()).pipe(gulp.dest('./build/images'))",
    "comprimeJavaScript": "gulp.src('./source/scripts/*.js').pipe(uglify()).pipe(obfuscate()).pipe(gulp.dest('./build/scripts'))",
    "compilaSass": "gulp.src('./source/styles/main.scss').pipe(sourcemaps.init()).pipe(sass({outputStyle: 'compressed'})).pipe(sourcemaps.write('./maps')).pipe(gulp.dest('./build/styles'))",
    "default": "gulp.watch('./source/styles/*.scss', {ignoreInitial: false}, gulp.series(compilaSass)); gulp.watch('./source/scripts/*.js', {ignoreInitial: false}, gulp.series(comprimeJavaScript)); gulp.watch('./source/images/*', {ignoreInitial: false}, gulp.series(comprimeImagens))"
  }
}
